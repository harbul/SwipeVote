node_modules/
dist/
.DS_Store
*.log

# Local DB & WAL files — re-create via `node seed.js --reset`
server/data/*.db
server/data/*.db-wal
server/data/*.db-shm

# Claude Code transient worktrees (operational, never committed)
.claude/worktrees/
